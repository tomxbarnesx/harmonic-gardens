<%# CURRENTLY ON ARCHIVES POSTS %>
if (document.getElementById("invoice-<%= @invoice.id %>")) {
    $('#invoice-<%= @invoice.id %>').fadeOut(250);
} else {
    $(window.location.replace("/invoices"))
}
$('#flash').html('<%= j render partial: "layouts/flash_messages" %>');