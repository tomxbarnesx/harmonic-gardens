$("#add-form").empty().css("display", "none");
$("#day-log-container").empty();

<% if @shifts.length >= 1 %>
    $("#day-log-container").append("<%= j (render 'day_log_list') %>")
<% else %>
    $("#day-log-container").append("<%= j (render 'day_log_list_none') %>")
<% end %>

<% if @material_dates.length >= 1 %>
    $("#day-log-container").append("<%= j (render 'day_materials_list') %>")
<% else %>
    $("#day-log-container").append("<%= j (render 'day_materials_list_none') %>")
<% end %>

$("#day-log-container").css("display", "block");

$(document).ready( function () {
    $('#datatable').DataTable( {
        "paging": false,
        "info": false,
        "bLengthChange": false,
        columnDefs: [
            { orderable: false, targets: -1 },
         ],
        "initComplete": function(){ 
            $("#datatable").show(); 
            }
    } );
} );

$(document).ready( function () {
    $('#datatable2').DataTable( {
        "paging": false,
        "info": false,
        "bLengthChange": false,
        columnDefs: [
            { orderable: false, targets: -1 },
         ],
        "initComplete": function(){ 
            $("#datatable2").show(); 
            }
    } );
} );